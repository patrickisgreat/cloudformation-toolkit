# `containers/alb`

An Application Load Balancer that routes nothing until a service asks it to.

## Usage

```bash
./bin/cfn deploy containers/alb --env dev \
  --param VpcId=vpc-abc --param SubnetIds=subnet-a,subnet-b
```

Then wire a service to it — `containers/fargate-service` takes `ListenerArn` and
attaches its own rule:

```yaml
Service:
  Type: AWS::CloudFormation::Stack
  Properties:
    TemplateURL: ../../templates/containers/fargate-service/template.yaml
    Parameters:
      ListenerArn: !GetAtt Alb.Outputs.ListenerArn
      AlbSecurityGroupId: !GetAtt Alb.Outputs.SecurityGroupId
```

## The default action is a 404, on purpose

Both listeners default to a fixed `404`, not a forward. This is the main design
decision in the template.

A load balancer whose default action forwards to some service means **every**
unmatched request lands there: scanner traffic, requests for hostnames you no
longer serve, and requests for a service you deleted last month. Services attach
a `ListenerRule` with a host or path condition, so routing is explicit and
removing a service actually removes its route.

Use the `ListenerArn` output rather than `HttpsListenerArn` or `HttpListenerArn`
— it resolves to whichever listener exists, so a service does not need to know
whether TLS is configured.

## TLS

`CertificateArn` empty means **plain HTTP only**. That is fine for a scratch
environment and wrong for anything that handles a cookie, a token, or a
credential. With a certificate:

- port 443 serves, using `SslPolicy` (default: TLS 1.3 and 1.2 only),
- port 80 issues a `301` preserving host, path, and query.

Get a certificate from `networking/acm-certificate` — DNS-validated, so it
renews without anyone touching it.

## `IdleTimeoutSeconds` is the one that bites

The 60-second default is shorter than a slow endpoint. A streaming LLM response,
a large report, or a slow upload that exceeds it surfaces as a **504 with no
corresponding backend error**, which is a genuinely confusing hour of debugging.
If you serve long responses, raise this and set your server's keep-alive timeout
*higher* than the ALB's — if the backend closes first, the ALB reports a 502.

## Access logs

`AccessLogsBucket` expects a bucket whose policy already allows ELB log
delivery. In regions created after August 2022, that means granting the
`logdelivery.elasticloadbalancing.amazonaws.com` service principal
`s3:PutObject`; older regions require the region's ELB account ID as the
principal. `data/s3-bucket` has an `AccessLogDelivery` bucket-policy mode that
sets this up.

## Egress is not restricted

The security group declares ingress only. CloudFormation removes the default
allow-all egress rule as soon as `SecurityGroupEgress` is present, and the ALB
must reach targets on whatever port they listen on — which this template does not
know. Restriction belongs on the target's security group, and
`containers/fargate-service` scopes its ingress to this group specifically.

See [`examples/basic`](examples/basic) for a parameter set you can deploy as-is.

<!-- BEGIN_CFN_DOCS -->
<!-- Generated by `./bin/cfn docs`. Do not edit between these markers. -->

### Parameters

| Parameter | Description | Type | Default |
|-----------|-------------|------|---------|
| `NamePrefix` | Prefix for the load balancer and security group names. | `String` | **required** |
| `Environment` | Environment this load balancer serves.<br>Allowed: `dev`, `staging`, `prod` | `String` | `dev` |
| `VpcId` | VPC the load balancer and its security group live in. | `AWS::EC2::VPC::Id` | **required** |
| `SubnetIds` | At least two subnets in different AZs - the ALB service requires it. Use public subnets for an internet-facing scheme and private subnets for an internal one. | `List<AWS::EC2::Subnet::Id>` | **required** |
| `Scheme` | internet-facing gets public IPs and must sit in public subnets. internal is reachable only from inside the VPC (and anything peered to it).<br>Allowed: `internet-facing`, `internal` | `String` | `internet-facing` |
| `IngressCidr` | Source range allowed to reach ports 80 and 443. 0.0.0.0/0 is correct for a public site; narrow it for an internal load balancer or an admin surface. | `String` | `0.0.0.0/0` |
| `CertificateArn` | ACM certificate ARN for the HTTPS listener. Leave empty to serve plain HTTP only - acceptable for a scratch environment, never for anything handling a cookie or a credential. | `String` | `""` |
| `SslPolicy` | Negotiation policy for the HTTPS listener. The default supports TLS 1.3 and 1.2 only; the -Res- variants additionally drop the weaker 1.2 cipher suites at the cost of very old client support.<br>Allowed: `ELBSecurityPolicy-TLS13-1-2-2021-06`, `ELBSecurityPolicy-TLS13-1-2-Res-2021-06`, `ELBSecurityPolicy-TLS13-1-3-2021-06`, `ELBSecurityPolicy-FS-1-2-Res-2020-10` | `String` | `ELBSecurityPolicy-TLS13-1-2-2021-06` |
| `RedirectHttpToHttps` | Make port 80 issue a 301 to HTTPS instead of serving content. Only takes effect when a certificate is supplied.<br>Allowed: `true`, `false` | `String` | `true` |
| `IdleTimeoutSeconds` | How long an idle connection is held open. Raise it above your slowest endpoint's response time - an LLM streaming response or a long report can exceed the 60s default and surface as a 504 with no backend error. | `Number` | `60` |
| `EnableHttp2` | HTTP/2 for clients. Note this is separate from backend protocol: gRPC services need the target group's ProtocolVersion set to GRPC, which containers/fargate-service exposes.<br>Allowed: `true`, `false` | `String` | `true` |
| `DesyncMitigationMode` | How strictly to reject ambiguous HTTP requests. `defensive` blocks the known request-smuggling shapes while tolerating merely non-compliant clients; `strictest` rejects anything non-compliant.<br>Allowed: `defensive`, `strictest`, `monitor` | `String` | `defensive` |
| `EnableDeletionProtection` | Refuse to delete the load balancer. Worth turning on in prod: deleting an ALB releases its DNS name, and anything with that name cached keeps resolving to nothing.<br>Allowed: `true`, `false` | `String` | `false` |
| `AccessLogsBucket` | S3 bucket for access logs. The bucket policy must already allow ELB log delivery - see the README. Leave empty to disable. | `String` | `""` |
| `AccessLogsPrefix` | Key prefix within the access log bucket. | `String` | `alb` |
| `WebAclArn` | WAFv2 web ACL ARN to associate, from networking/waf-web-acl. Leave empty for none. | `String` | `""` |

### Outputs

| Output | Description | Exported as |
|--------|-------------|-------------|
| `LoadBalancerArn` | ARN of the load balancer. | — |
| `LoadBalancerDnsName` | DNS name to point a Route 53 alias record at. Do not hand this out directly - it changes if the load balancer is replaced. | — |
| `LoadBalancerFullName` | Full name (app/<name>/<id>). Application Auto Scaling needs this, combined with a target group's full name, to build the ALBRequestCountPerTarget resource label — request-based scaling cannot be configured without it. | — |
| `LoadBalancerHostedZoneId` | Canonical hosted zone ID, required by a Route 53 alias record. | — |
| `SecurityGroupId` | Security group of the load balancer. A service's own security group should allow ingress from this group and nothing else. | — |
| `HttpListenerArn` | ARN of the port 80 listener. | — |
| `HttpsListenerArn` | ARN of the port 443 listener, when a certificate was supplied. This is what a service attaches its ListenerRule to. | — |
| `ListenerArn` | The listener a service should attach rules to - HTTPS when a certificate exists, HTTP otherwise. Wire this into fargate-service's ListenerArn so the service does not need to know which one exists. | — |

### Resources

| Logical ID | Type | Created when |
|------------|------|--------------|
| `SecurityGroup` | `AWS::EC2::SecurityGroup` | always |
| `LoadBalancer` | `AWS::ElasticLoadBalancingV2::LoadBalancer` | always |
| `HttpListener` | `AWS::ElasticLoadBalancingV2::Listener` | always |
| `HttpsListener` | `AWS::ElasticLoadBalancingV2::Listener` | `HasCertificate` |
| `WebAclAssociation` | `AWS::WAFv2::WebACLAssociation` | `HasWebAcl` |
<!-- END_CFN_DOCS -->

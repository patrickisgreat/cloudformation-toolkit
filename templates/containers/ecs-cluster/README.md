# `containers/ecs-cluster`

An ECS cluster with both Fargate capacity providers registered, Container
Insights on, and ECS Exec sessions written to an audit log group.

An ECS cluster is a cheap, almost-empty object — it costs nothing on its own. One
cluster per environment is the normal granularity; you do not need one per
service.

## Usage

```bash
./bin/cfn deploy containers/ecs-cluster --env dev
```

## The capacity strategy is the interesting part

Three numbers decide how much you pay and how much interruption you accept:

| Parameter | Meaning |
|-----------|---------|
| `OnDemandBaseCount` | Tasks placed on on-demand **before weights apply at all** |
| `OnDemandWeight` | Share of the remainder on on-demand |
| `SpotWeight` | Share of the remainder on Spot |

`OnDemandBaseCount` is your interruption floor, and it is the parameter people
get wrong. With `Base: 1, OnDemandWeight: 1, SpotWeight: 3`, a service running 9
tasks places 1 on-demand, then splits the remaining 8 one-to-three — 2 more
on-demand and 6 Spot. One task is guaranteed to survive a Spot reclamation event.

**Fargate Spot is ~70% cheaper and can be reclaimed with a two-minute warning.**
That is a good trade for a stateless HTTP service behind a load balancer, which
drains and reschedules. It is a bad trade for:

- a queue consumer partway through a long batch,
- anything holding a long-lived connection (WebSocket, gRPC stream, SSE),
- a job with no checkpointing.

`SpotWeight` defaults to `0` — Spot is opt-in, because the failure it introduces
is invisible until the day capacity gets tight.

Both capacity providers are registered even when `SpotWeight` is 0, so moving a
service onto Spot later is a parameter change rather than a cluster rebuild.

## ECS Exec is a shell in production

`EnableExecuteCommandLogging` defaults to on, writing every session to
`/<env>/<prefix>/ecs-exec` with a 90-day retention and `DeletionPolicy: Retain`.
ECS Exec gives an operator an interactive shell inside a running production
container; without this log group there is no record of what was typed.

Note that the cluster only makes logging *possible*. Each service must also set
`EnableExecuteCommand: true`, and its task role needs the `ssmmessages`
permissions — `containers/fargate-service` wires both.

## Service Connect vs a load balancer

`CreateServiceConnectNamespace` creates a Cloud Map namespace so services address
each other as `http://orders/` with client-side retries, per-call metrics, and no
load balancer in the path. It requires `VpcId`.

Off by default: for a single service there is nothing to connect to, and the
namespace becomes an unused resource that blocks VPC deletion.

See [`examples/basic`](examples/basic) for a parameter set you can deploy as-is.

<!-- BEGIN_CFN_DOCS -->
<!-- Generated by `./bin/cfn docs`. Do not edit between these markers. -->

### Parameters

| Parameter | Description | Type | Default |
|-----------|-------------|------|---------|
| `NamePrefix` | Prefix for the cluster name and namespace. | `String` | **required** |
| `Environment` | Environment this cluster serves.<br>Allowed: `dev`, `staging`, `prod` | `String` | `dev` |
| `OnDemandBaseCount` | Tasks placed on on-demand Fargate before the weights apply at all. This is your interruption floor: set it to the number of tasks that must survive a Spot reclamation. Zero means every task is interruptible. | `Number` | `1` |
| `OnDemandWeight` | Relative share of tasks above the base placed on on-demand Fargate. | `Number` | `1` |
| `SpotWeight` | Relative share placed on Fargate Spot, which is around 70% cheaper and can be reclaimed with a two-minute warning. Zero disables Spot. A stateless HTTP service behind a load balancer handles reclamation well; a queue consumer mid-batch or anything holding a long-lived connection does not. | `Number` | `0` |
| `ContainerInsights` | Per-task CPU, memory, network and storage metrics. `enabled` is the classic metric set. `enhanced` adds per-container detail and costs meaningfully more on a large cluster. `disabled` saves money and leaves you diagnosing a memory leak by guesswork.<br>Allowed: `enabled`, `enhanced`, `disabled` | `String` | `enabled` |
| `EnableExecuteCommandLogging` | Record every ECS Exec session to CloudWatch Logs. ECS Exec is an interactive shell inside a running production container; without this, there is no record of what was typed.<br>Allowed: `true`, `false` | `String` | `true` |
| `LogRetentionDays` | Retention for the ECS Exec audit log group.<br>Allowed: `1`, `3`, `5`, `7`, `14`, `30`, `60`, `90`, `120`, `150`, `180`, `365`, `400`, `545`, `731`, `1827`, `3653` | `Number` | `90` |
| `CreateServiceConnectNamespace` | Create a Cloud Map namespace so services in this cluster can address each other by name (http://orders/) with retries and per-call metrics, instead of through a load balancer.<br>Allowed: `true`, `false` | `String` | `false` |
| `ServiceConnectNamespaceName` | DNS namespace for Service Connect, e.g. "internal". Services become reachable as <service>.<namespace>. | `String` | `internal` |
| `VpcId` | VPC the Service Connect namespace resolves inside. Required only when CreateServiceConnectNamespace is true; a private DNS namespace has to be attached to exactly one VPC. | `String` | `""` |

### Outputs

| Output | Description | Exported as |
|--------|-------------|-------------|
| `ClusterName` | Cluster name, for the ECS service's Cluster property and the CLI. | — |
| `ClusterArn` | Cluster ARN, for IAM policies and cross-stack references. | — |
| `ServiceConnectNamespaceArn` | Cloud Map namespace ARN, for a service's ServiceConnectConfiguration. | — |
| `ServiceConnectNamespaceName` | DNS namespace services are addressable under. | — |
| `ExecuteCommandLogGroupName` | Log group holding the ECS Exec session audit trail. | — |

### Resources

| Logical ID | Type | Created when |
|------------|------|--------------|
| `ExecuteCommandLogGroup` | `AWS::Logs::LogGroup` | `ShouldLogExecSessions` |
| `Cluster` | `AWS::ECS::Cluster` | always |
| `ServiceConnectNamespace` | `AWS::ServiceDiscovery::PrivateDnsNamespace` | `ShouldCreateNamespace` |
<!-- END_CFN_DOCS -->

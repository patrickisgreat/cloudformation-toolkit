# `data/kinesis-stream`

A Kinesis Data Stream: encrypted, retention-configured, and by default in
on-demand capacity mode so nobody has to predict a shard count.

## Usage

```bash
./bin/cfn deploy data/kinesis-stream --env dev --param StreamSuffix=clickstream
```

Feed it into a lake with [`data/firehose-to-s3`](../firehose-to-s3), or attach a
Lambda event source mapping to process records directly.

## `CapacityMode` is the cost decision

**`ON_DEMAND`** (default) scales automatically and bills per GB written and read.
No shard management, no resharding, no throughput exceptions. For spiky or
unknown load it is both simpler and cheaper.

**`PROVISIONED`** bills per shard-hour and becomes cheaper only above roughly a
sustained 1 MB/s per shard — but you have to predict the shard count, and being
wrong produces `ProvisionedThroughputExceededException` on the producer side.
Most SDKs retry that transparently, so the symptom is not an error: it is a
growing backlog and rising write latency that nobody attributes to sharding.

Each shard takes 1 MB/s or 1000 records/s in, and 2 MB/s out shared across all
standard consumers.

## `RetentionHours` is your recovery window

24 hours (the AWS default) is how long you have to notice and fix a broken
consumer before the unread data is gone. 168 hours (7 days) costs extra and turns
a weekend outage into a replay rather than an incident report. Anything you
cannot reconstruct from a source of truth deserves the longer window.

## Enhanced fan-out

Standard consumers **share** the stream's 2 MB/s per shard read capacity. Two or
three consumers are fine; beyond that they start starving each other and
propagation delay climbs.

`EnhancedFanOutConsumerName` registers a consumer with its own dedicated 2 MB/s
per shard, billed hourly per shard. Add it when you have a consumer whose latency
matters and others competing with it.

See [`examples/basic`](examples/basic) for a parameter set you can deploy as-is.

<!-- BEGIN_CFN_DOCS -->
<!-- Generated by `./bin/cfn docs`. Do not edit between these markers. -->

### Parameters

| Parameter | Description | Type | Default |
|-----------|-------------|------|---------|
| `NamePrefix` | Prefix for the stream name. | `String` | **required** |
| `Environment` | Environment this stream belongs to.<br>Allowed: `dev`, `staging`, `prod` | `String` | `dev` |
| `StreamSuffix` | What this stream carries, e.g. "clickstream" or "telemetry". | `String` | `events` |
| `CapacityMode` | ON_DEMAND scales automatically and bills per GB written and read, with no shard management - the right default, and much cheaper for spiky or unknown load. PROVISIONED bills per shard-hour and is cheaper only above roughly a sustained 1 MB/s per shard, but requires you to predict the shard count and to reshard when you are wrong.<br>Allowed: `ON_DEMAND`, `PROVISIONED` | `String` | `ON_DEMAND` |
| `ShardCount` | Shards, when CapacityMode is PROVISIONED. Each shard takes 1 MB/s or 1000 records/s in and 2 MB/s out. Exceeding it returns ProvisionedThroughputExceededException to the producer, which most SDKs retry into a backlog rather than surfacing. | `Number` | `1` |
| `RetentionHours` | How long records stay readable. 24 hours is the AWS default and is the window you have to fix a broken consumer before data is gone; 168 (7 days) costs extra but turns a weekend outage into a replay. | `Number` | `24` |
| `KmsKeyId` | KMS key ID, ARN or alias for server-side encryption. Defaults to the AWS-managed Kinesis key, which is free. | `String` | `alias/aws/kinesis` |
| `EnhancedFanOutConsumerName` | Register a dedicated-throughput consumer with this name. Each enhanced fan-out consumer gets its own 2 MB/s per shard instead of sharing the stream's read capacity, and is billed hourly per shard. Leave empty to share read throughput, which is fine up to two or three consumers. | `String` | `""` |

### Outputs

| Output | Description | Exported as |
|--------|-------------|-------------|
| `StreamName` | Stream name, for the SDK and for CloudWatch alarm dimensions. | — |
| `StreamArn` | Stream ARN, for IAM policies, Firehose sources and Lambda event sources. | — |
| `ConsumerArn` | Enhanced fan-out consumer ARN, when one was registered. | — |

### Resources

| Logical ID | Type | Created when |
|------------|------|--------------|
| `Stream` | `AWS::Kinesis::Stream` | always |
| `Consumer` | `AWS::Kinesis::StreamConsumer` | `HasEnhancedFanOut` |
<!-- END_CFN_DOCS -->

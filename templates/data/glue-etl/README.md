# `data/glue-etl`

The catalog and transform half of a data lake: a Glue database, a crawler that
discovers schema and partitions, and an optional Spark ETL job.

Pair it with [`data/firehose-to-s3`](../firehose-to-s3) for ingestion and
[`data/athena-workgroup`](../athena-workgroup) for query.

## Usage

```bash
./bin/cfn deploy data/glue-etl --env dev \
  --param SourceBucketArn=<lake-bucket-arn> --param SourcePrefix=raw/clickstream
```

## Point the crawler at the table root

`SourcePrefix` must be the directory *above* the partition folders:

```
raw/clickstream/          <- point here
  year=2026/month=08/...
```

A crawler pointed inside the partitions registers **each partition as a separate
table**, which produces a catalog with hundreds of one-day tables and no obvious
way back.

## Schema changes are logged, not applied

`UpdateBehavior: LOG` and `DeleteBehavior: LOG` are deliberate. The tempting
alternative, `UPDATE_IN_DATABASE`, silently rewrites the table definition when a
column's inferred type changes — and every query that relied on the old type
starts failing, or worse, silently coercing. `DEPRECATE_IN_DATABASE` on delete is
worse still: a lifecycle rule expiring old partitions looks to the crawler like a
table that should disappear.

You will see schema drift in the crawler's log and decide what to do about it.

## `RecrawlBehavior: CRAWL_NEW_FOLDERS_ONLY`

A full recrawl lists every object in the lake, which is slow and billed per
object. New-folders-only is what makes a daily crawl on a large lake affordable.
If you change the shape of existing data, run a full crawl once by hand.

## Job bookmarks

`--job-bookmark-option: job-bookmark-enable` makes a rerun process only data that
arrived since the last successful run. Without it, an hourly job reprocesses the
entire lake every hour — the single most common Glue cost surprise.

This is also why `MaxConcurrentRuns` defaults to 1: two concurrent runs with
bookmarks enabled will process overlapping data.

## `JobTimeoutMinutes`

Defaults to 60. Glue's own default is **2880 minutes — two days** — which on a
hung job is an expensive way to find out something is wrong.

## Cost

Glue bills per DPU-hour with a one-minute minimum. `G.1X` (4 vCPU / 16 GB) at 2
workers is the right starting point; scale up only after seeing the job actually
spill.

See [`examples/basic`](examples/basic) for a parameter set you can deploy as-is.

<!-- BEGIN_CFN_DOCS -->
<!-- Generated by `./bin/cfn docs`. Do not edit between these markers. -->

### Parameters

| Parameter | Description | Type | Default |
|-----------|-------------|------|---------|
| `NamePrefix` | Prefix for the database, crawler and job names. | `String` | **required** |
| `Environment` | Environment this catalog serves.<br>Allowed: `dev`, `staging`, `prod` | `String` | `dev` |
| `DatabaseSuffix` | Catalog database name suffix, e.g. "raw" or "curated". Glue database names use underscores, so hyphens in NamePrefix are converted. | `String` | `raw` |
| `SourceBucketArn` | ARN of the bucket the crawler reads and the job takes input from. | `String` | **required** |
| `SourcePrefix` | Prefix the crawler scans, e.g. "raw/clickstream". Point it at the table root, above the partition directories - a crawler pointed inside the partitions registers each one as a separate table. | `String` | `raw` |
| `TargetBucketArn` | ARN of the bucket the ETL job writes to. May be the same bucket. | `String` | `""` |
| `TargetPrefix` | Prefix the job writes transformed data under. | `String` | `curated` |
| `EnableCrawler` | Create the crawler that discovers schema and partitions.<br>Allowed: `true`, `false` | `String` | `true` |
| `CrawlerSchedule` | Cron expression for the crawler, in UTC, e.g. "cron(0 1 * * ? *)" for 01:00 daily. Leave empty to run it on demand only. | `String` | `cron(0 1 * * ? *)` |
| `EnableJob` | Create the Spark ETL job. Off by default - a catalog and a crawler are useful on their own, and a job with no script is just a failing resource.<br>Allowed: `true`, `false` | `String` | `false` |
| `JobScriptS3Uri` | s3:// URI of the PySpark or Scala script. Version the key: Glue reads the script at run time, so overwriting it changes what runs without any CloudFormation event to show for it. | `String` | `""` |
| `GlueVersion` | Glue runtime version, which fixes the Spark and Python versions.<br>Allowed: `3.0`, `4.0`, `5.0` | `String` | `4.0` |
| `WorkerType` | G.1X is 4 vCPU / 16 GB per worker and the sensible starting point. G.2X doubles both, for wide joins or skewed data. G.025X is a quarter worker for small streaming jobs.<br>Allowed: `G.1X`, `G.2X`, `G.4X`, `G.8X`, `G.025X` | `String` | `G.1X` |
| `NumberOfWorkers` | Workers per run. Glue bills per DPU-hour with a 1-minute minimum, so over-provisioning a short job is a direct multiplier on cost. | `Number` | `2` |
| `JobTimeoutMinutes` | Kill the run after this long. Glue's own default is 2880 minutes - two days - which on a hung job is an expensive way to find out. | `Number` | `60` |
| `MaxConcurrentRuns` | Simultaneous runs allowed. Keep it at 1 unless the job is genuinely partitioned by input: two concurrent runs with bookmarks enabled will process overlapping data. | `Number` | `1` |
| `LogRetentionDays` | Retention for the job's log group.<br>Allowed: `1`, `3`, `5`, `7`, `14`, `30`, `60`, `90`, `120`, `150`, `180`, `365`, `400`, `545`, `731`, `1827`, `3653` | `Number` | `30` |

### Outputs

| Output | Description | Exported as |
|--------|-------------|-------------|
| `DatabaseName` | Glue Data Catalog database name, for Athena queries and job scripts. | — |
| `ServiceRoleArn` | Role the crawler and job assume. | — |
| `CrawlerName` | Crawler name, for on-demand runs via start-crawler. | — |
| `JobName` | ETL job name, for start-job-run and for Step Functions. | — |

### Resources

| Logical ID | Type | Created when |
|------------|------|--------------|
| `Database` | `AWS::Glue::Database` | always |
| `ServiceRole` | `AWS::IAM::Role` | always |
| `Crawler` | `AWS::Glue::Crawler` | `ShouldCreateCrawler` |
| `JobLogGroup` | `AWS::Logs::LogGroup` | `ShouldCreateJob` |
| `Job` | `AWS::Glue::Job` | `ShouldCreateJob` |
<!-- END_CFN_DOCS -->

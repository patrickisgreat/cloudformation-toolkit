# `serverless/lambda-function`

A Lambda function packaged as a zip or a container image, with an execution role,
a log group whose retention is actually bounded, and the concurrency controls that
protect whatever it calls.

## Usage

```bash
./bin/cfn deploy serverless/lambda-function --env dev \
  --param CodeS3Bucket=my-artifacts --param CodeS3Key=webhook/abc1234.zip
```

## Any language

`PackageType: Zip` with `Runtime: provided.al2023` runs a compiled binary — Rust,
Go, C++, anything with a `bootstrap` entry point. `PackageType: Image` takes an
ECR image and lifts every runtime restriction, which is what you want for a
Python function with heavy native dependencies that will not fit in the 250 MB
unzipped zip limit.

**Put a commit SHA in `CodeS3Key`.** Lambda caches by bucket and key, so
overwriting the same key does not reliably update the running code — a deploy
that appears to succeed and changes nothing.

## `MemorySize` is the CPU dial

Lambda allocates CPU in proportion to memory. A CPU-bound function is frequently
*cheaper* at 1024 MB than at 512, because it finishes more than twice as fast.
Tune it by measuring duration at several sizes, not by guessing at memory
footprint.

`Architecture` defaults to `arm64` — roughly 20% cheaper per GB-second and
usually faster. Switch to `x86_64` only when a dependency has no arm64 build.

## `ReservedConcurrency` is a circuit breaker

Default `-1` (unreserved). Set it whenever the function talks to a database:
Lambda will happily scale to a thousand concurrent executions, and a thousand
connections is how a Lambda takes down the RDS instance it was reading from.

Note `0` does not mean "unlimited" — it disables the function entirely.

## The log group is declared, not inherited

Lambda creates `/aws/lambda/<name>` implicitly on first invocation, with
**infinite retention**, and that group is not owned by your stack. This template
declares it (30-day default, `Retain`) and orders the function after it. Without
that ordering you can end up with an unmanaged group and a stack that fails on
the next update with "log group already exists".

## VPC attachment is opt-in

Leave `SubnetIds` empty unless the function must reach something inside the VPC.
A VPC-attached function reaches AWS APIs only through a NAT gateway or an
interface endpoint, so attaching one "for security" usually just adds cost and a
new failure mode.

When you do attach, the execution role automatically swaps to
`AWSLambdaVPCAccessExecutionRole`. Without those ENI permissions the function
fails to create its network interface and every invocation times out **with no
log output at all** — one of the more confusing failures Lambda produces.

## Failure destinations

`FailureDestinationArn` sends the payload of an asynchronous invocation that
exhausted its retries to an SQS queue or SNS topic. Without one, the event is
gone: a log line is all that remains of it. For anything triggered by S3,
EventBridge, or SNS, this is the difference between a recoverable incident and a
silent one.

## Known limitation: environment variable slots

CloudFormation's `Fn::If` can substitute a *value* in a map but cannot add or
remove a *key*. Unused `EnvVarN` slots therefore appear as empty strings rather
than disappearing. Read configuration from SSM Parameter Store at startup if you
need many variables.

See [`examples/basic`](examples/basic) for a parameter set you can deploy as-is.

<!-- BEGIN_CFN_DOCS -->
<!-- Generated by `./bin/cfn docs`. Do not edit between these markers. -->

### Parameters

| Parameter | Description | Type | Default |
|-----------|-------------|------|---------|
| `NamePrefix` | Prefix for the function name. | `String` | **required** |
| `Environment` | Environment this function runs in. Injected as ENVIRONMENT.<br>Allowed: `dev`, `staging`, `prod` | `String` | `dev` |
| `FunctionSuffix` | What this function does, e.g. "resize" or "webhook". | `String` | `handler` |
| `PackageType` | Zip takes CodeS3Bucket/CodeS3Key and a managed runtime. Image takes ImageUri from ECR and lets you ship any runtime - Rust, a compiled Go binary with system libraries, or a Python function with heavy native deps that will not fit in a 250 MB unzipped layer.<br>Allowed: `Zip`, `Image` | `String` | `Zip` |
| `ImageUri` | ECR image URI, used when PackageType is Image. | `String` | `""` |
| `CodeS3Bucket` | Bucket holding the deployment zip, used when PackageType is Zip. | `String` | `""` |
| `CodeS3Key` | Key of the deployment zip. Include a version or commit SHA in the key - Lambda caches by bucket and key, so overwriting the same key does not reliably update the function. | `String` | `""` |
| `Runtime` | Managed runtime, for Zip packages. provided.al2023 is the one to use for Rust, Go, and anything else that compiles to a static binary.<br>Allowed: `python3.12`, `python3.11`, `nodejs20.x`, `nodejs22.x`, `java21`, `dotnet8`, `ruby3.3`, `provided.al2023` | `String` | `python3.12` |
| `Handler` | Entry point, for Zip packages, e.g. "app.handler" or "bootstrap". | `String` | `app.handler` |
| `Architecture` | arm64 (Graviton) is roughly 20% cheaper per GB-second and usually faster. Use x86_64 only when a dependency has no arm64 build.<br>Allowed: `arm64`, `x86_64` | `String` | `arm64` |
| `MemorySize` | Memory in MB. This is also the CPU dial - Lambda allocates CPU in proportion, so a CPU-bound function is often *cheaper* at higher memory because it finishes proportionally faster. | `Number` | `512` |
| `TimeoutSeconds` | Maximum run time. Keep it close to the real worst case: a function that hangs bills for the full timeout on every stuck invocation. | `Number` | `30` |
| `EphemeralStorageMb` | Size of /tmp. Raise it for functions that download models or large files. | `Number` | `512` |
| `ReservedConcurrency` | Cap on simultaneous executions, and the only protection a downstream database has against a Lambda scaling to a thousand connections. -1 leaves it unreserved (shares the account pool); 0 disables the function entirely. | `Number` | `-1` |
| `ProvisionedConcurrency` | Pre-initialised execution environments, which removes cold starts and bills whether or not they are used. 0 disables. Only meaningful on an alias or version, so this template publishes a version when it is non-zero. | `Number` | `0` |
| `EnvVar1Name` | Name of an additional environment variable. Leave empty to skip. | `String` | `""` |
| `EnvVar1Value` | Value for EnvVar1Name. Not for credentials - read those from Secrets Manager at runtime. | `String` | `""` |
| `EnvVar2Name` | Name of a second additional environment variable. | `String` | `""` |
| `EnvVar2Value` | Value for EnvVar2Name. | `String` | `""` |
| `SubnetIds` | Private subnets to attach to, for reaching an RDS instance or an ElastiCache cluster. Leave empty to run outside the VPC - which is faster to scale and is the right default, since a VPC-attached function reaches AWS APIs only through NAT or an endpoint. | `CommaDelimitedList` | `""` |
| `SecurityGroupIds` | Security groups for VPC attachment. Required when SubnetIds is set. | `CommaDelimitedList` | `""` |
| `LogRetentionDays` | Retention for the log group. Declared here rather than left to Lambda, which creates the group implicitly with infinite retention.<br>Allowed: `1`, `3`, `5`, `7`, `14`, `30`, `60`, `90`, `120`, `150`, `180`, `365`, `400`, `545`, `731`, `1827`, `3653` | `Number` | `30` |
| `TracingMode` | X-Ray tracing. Active samples traces; PassThrough only continues an existing trace.<br>Allowed: `Active`, `PassThrough` | `String` | `Active` |
| `FailureDestinationArn` | SQS queue or SNS topic receiving the payload of asynchronous invocations that exhaust their retries. Without one, a failed async invocation is gone - the log line is all that is left of the event. | `String` | `""` |
| `ManagedPolicyArn` | Managed policy granting the function the AWS access it needs, beyond logging and X-Ray. Leave empty for a function that calls no AWS APIs. | `String` | `""` |

### Outputs

| Output | Description | Exported as |
|--------|-------------|-------------|
| `FunctionArn` | Function ARN, for event source mappings and API integrations. | — |
| `FunctionName` | Function name, for the CLI and CloudWatch alarm dimensions. | — |
| `ExecutionRoleArn` | Execution role ARN - the identity the function runs as. | — |
| `LogGroupName` | Log group the function writes to. | — |

### Resources

| Logical ID | Type | Created when |
|------------|------|--------------|
| `LogGroup` | `AWS::Logs::LogGroup` | always |
| `ExecutionRole` | `AWS::IAM::Role` | always |
| `Function` | `AWS::Lambda::Function` | always |
| `AsyncConfig` | `AWS::Lambda::EventInvokeConfig` | `HasFailureDestination` |
| `Version` | `AWS::Lambda::Version` | `HasProvisionedConcurrency` |
<!-- END_CFN_DOCS -->

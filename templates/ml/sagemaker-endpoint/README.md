# `ml/sagemaker-endpoint`

A SageMaker inference endpoint — real-time or serverless — with autoscaling on
the metric that actually matters, and a deployment that rolls back when the new
model starts erroring.

For self-hosted open-weight LLMs on your own GPU fleet, see
[`ml/gpu-inference-service`](../gpu-inference-service) instead.

## Usage

```bash
./bin/cfn deploy ml/sagemaker-endpoint --env dev \
  --param InferenceImageUri=<dlc-image> \
  --param HuggingFaceModelId=sentence-transformers/all-MiniLM-L6-v2
```

## `ServingMode`

**`SERVERLESS`** scales to zero and bills per request. The cost is a cold start
of tens of seconds on the first request after idle, and **no GPU support at
all**. Right for embeddings, classifiers, and anything with bursty low volume.

**`REALTIME`** provisions instances billed continuously, responds in
milliseconds, and supports GPU. Right for anything latency-sensitive or
GPU-bound.

`InitialInstanceCount` defaults to 1, which is fine for dev — but understand that
with one instance, every deployment and every instance failure is an outage.

## Scale on invocations, not CPU

`TargetInvocationsPerInstance` uses
`SageMakerVariantInvocationsPerInstance`, not CPU utilisation. This matters more
than it sounds: **GPU inference is frequently memory-bound with CPU near idle**,
so a CPU-based scaling policy simply never fires while the endpoint queues.

`MaxInstanceCount` is the parameter that decides whether a traffic spike on a
`ml.g5.12xlarge` costs tens of dollars or thousands. Set it as a budget.

## Deployments roll back

`DeploymentConfig` shifts traffic to the new fleet, then holds the old one for 10
minutes with `InvocationErrorAlarm` watching. If the new model version errors, it
rolls back automatically.

Without this, a model that loads successfully but answers badly has already
replaced the working one, and recovery means redeploying the previous artifact by
hand.

## Data capture is how you evaluate later

`DataCaptureS3Uri` records a sample of inputs and outputs to S3. This is what
makes drift monitoring, offline evaluation, and "why did it answer that in
March?" possible. Without it those requests are simply gone.

It is off by default because captured inference data is a copy of whatever your
users sent — treat the destination bucket accordingly.

## Container contract

The image must implement `/ping` and `/invocations`. SageMaker health-checks
`/ping` before sending any traffic; a container that starts but never answers it
fails the deployment with a timeout rather than an error, which reads as a
networking problem and is not one.

See [`examples/basic`](examples/basic) for a parameter set you can deploy as-is.

<!-- BEGIN_CFN_DOCS -->
<!-- Generated by `./bin/cfn docs`. Do not edit between these markers. -->

### Parameters

| Parameter | Description | Type | Default |
|-----------|-------------|------|---------|
| `NamePrefix` | Prefix for the model, config and endpoint names. | `String` | **required** |
| `Environment` | Environment this endpoint serves.<br>Allowed: `dev`, `staging`, `prod` | `String` | `dev` |
| `EndpointSuffix` | Distinguishes endpoints, e.g. "embeddings" or "ranker". | `String` | `inference` |
| `InferenceImageUri` | Serving container image - a SageMaker Deep Learning Container, a Large Model Inference (LMI) image, or your own. It must implement /ping and /invocations; SageMaker health-checks the former before sending traffic. | `String` | **required** |
| `ModelDataUrl` | s3:// URI of model.tar.gz. Leave empty for images that download weights at startup (an LMI container pulling from Hugging Face, for instance) - though note that makes startup depend on an external service. | `String` | `""` |
| `HuggingFaceModelId` | Model to download at container start, e.g. "sentence-transformers/all- MiniLM-L6-v2". Passed as HF_MODEL_ID, which the Hugging Face and Large Model Inference containers use to fetch weights from the Hub. Leave empty when the weights come from ModelDataUrl instead. | `String` | `""` |
| `ServingMode` | REALTIME provisions instances that are billed continuously and respond in milliseconds. SERVERLESS scales to zero and bills per request, at the cost of a cold start of tens of seconds on the first request after idle - and it supports no GPU.<br>Allowed: `REALTIME`, `SERVERLESS` | `String` | `REALTIME` |
| `InstanceType` | Instance type for REALTIME. ml.g5.xlarge is the usual entry point for a small GPU model; ml.c6i.xlarge for CPU inference.<br>Allowed: `ml.t2.medium`, `ml.m5.large`, `ml.m5.xlarge`, `ml.c6i.xlarge`, `ml.c6i.2xlarge`, `ml.g5.xlarge`, `ml.g5.2xlarge`, `ml.g5.12xlarge`, `ml.inf2.xlarge` | `String` | `ml.m5.large` |
| `InitialInstanceCount` | Instances at creation. One instance means every deployment and every instance failure is an outage; two is the smallest number that is not. | `Number` | `1` |
| `ServerlessMemoryMb` | Memory for SERVERLESS mode. CPU scales with it, as on Lambda.<br>Allowed: `1024`, `2048`, `3072`, `4096`, `5120`, `6144` | `Number` | `2048` |
| `ServerlessMaxConcurrency` | Concurrent invocations allowed in SERVERLESS mode. | `Number` | `5` |
| `MinInstanceCount` | Autoscaling floor for REALTIME endpoints. | `Number` | `1` |
| `MaxInstanceCount` | Autoscaling ceiling. On GPU instance types this is the parameter that decides whether a traffic spike costs tens or thousands of dollars. | `Number` | `2` |
| `TargetInvocationsPerInstance` | Invocations per minute per instance the scaler holds. Unlike CPU utilisation, this tracks the thing that actually saturates an inference server - GPU inference is often memory-bound with CPU near idle, so CPU scaling never fires. | `Number` | `100` |
| `DataCaptureS3Uri` | s3:// prefix to record inputs and outputs to. This is what makes drift monitoring and offline evaluation possible later; without it those requests are gone. Leave empty to disable. | `String` | `""` |
| `DataCapturePercentage` | Percentage of requests captured. | `Number` | `20` |
| `ModelArtifactBucketArn` | Bucket the execution role may read model artifacts from. Required unless the container downloads weights itself. | `String` | `""` |
| `KmsKeyArn` | KMS key for endpoint storage volumes and captured data. | `String` | `""` |

### Outputs

| Output | Description | Exported as |
|--------|-------------|-------------|
| `EndpointName` | Endpoint name, for InvokeEndpoint and for alarm dimensions. | — |
| `EndpointArn` | Endpoint ARN, for IAM policies on the calling role. | — |
| `ModelName` | Model name registered with SageMaker. | — |
| `ExecutionRoleArn` | Role SageMaker assumes to pull the image and read model artifacts. | — |
| `ErrorAlarmName` | Alarm wired into deployment auto-rollback. | — |

### Resources

| Logical ID | Type | Created when |
|------------|------|--------------|
| `ExecutionRole` | `AWS::IAM::Role` | always |
| `Model` | `AWS::SageMaker::Model` | always |
| `EndpointConfig` | `AWS::SageMaker::EndpointConfig` | always |
| `Endpoint` | `AWS::SageMaker::Endpoint` | always |
| `InvocationErrorAlarm` | `AWS::CloudWatch::Alarm` | always |
| `ScalableTarget` | `AWS::ApplicationAutoScaling::ScalableTarget` | `IsRealtime` |
| `ScalingPolicy` | `AWS::ApplicationAutoScaling::ScalingPolicy` | `IsRealtime` |
<!-- END_CFN_DOCS -->

# `<group>/<name>`

One sentence on what this template provisions and when you would reach for it.

## Usage

Deploy it directly:

```bash
./bin/cfn deploy <group>/<name> --env dev
```

Or nest it inside a stack:

```yaml
Resources:
  Thing:
    Type: AWS::CloudFormation::Stack
    Properties:
      TemplateURL: ../../templates/<group>/<name>/template.yaml
      Parameters:
        NamePrefix: !Ref NamePrefix
        Environment: !Ref Environment
```

Explain the two or three decisions a caller actually has to make here — the
defaults that were chosen and why, and the escape hatch for the case the
defaults do not cover. The generated tables below say *what* the knobs are; this
section says *which ones matter*.

See [`examples/basic`](examples/basic) for a parameter set you can deploy as-is.

<!-- BEGIN_CFN_DOCS -->
<!-- Generated by `./bin/cfn docs`. Do not edit between these markers. -->
<!-- END_CFN_DOCS -->

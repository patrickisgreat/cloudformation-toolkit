# `database/aurora-serverless-v2`

An Aurora Serverless v2 cluster (PostgreSQL or MySQL) that is private,
encrypted, and whose master password never passes through CloudFormation.

## Usage

```bash
./bin/cfn deploy database/aurora-serverless-v2 --env dev \
  --param VpcId=vpc-abc --param SubnetIds=subnet-a,subnet-b \
  --param ClientSecurityGroupId=<task-security-group>
```

Then give the application the credentials without ever handling them — pass
`MasterSecretArn` to `containers/fargate-service`:

```
Secret1Name = DB_PASSWORD
Secret1Arn  = <MasterSecretArn>
Secret1JsonKey = password
```

## There is no password parameter

Secrets Manager generates the password, and the cluster reads it through a
`{{resolve:secretsmanager:...}}` dynamic reference at deploy time. The value
never appears in the template, the change set, the stack events, or a CI log.

The generated password excludes `"@/\'`$&;|<>()[]{}` — RDS rejects some of those
in a master password outright, and the rest break connection strings.

## Capacity is the cost dial

Aurora Serverless v2 bills per **ACU-hour** (~$0.12); one ACU is roughly 2 GiB of
memory with matching CPU.

- `MinCapacityAcu: 0` lets the cluster **pause entirely when idle** — excellent
  for dev, at the cost of a several-second cold start on the first query after a
  pause.
- `MinCapacityAcu: 0.5` is the lowest always-on setting.
- `MaxCapacityAcu` is your ceiling. At 16 ACUs sustained that is roughly
  $1,400/month, so pick it as a budget, not as a guess at peak.

## `ReaderCount` is really about failover

`0` readers is fine for dev. But a single-instance cluster **has no failover
target**: Aurora recovers by creating a new instance, which takes minutes. One
reader gives you a read endpoint and a standby that can be promoted in under a
minute.

`ReaderEndpoint` is safe to use unconditionally — with no readers it resolves to
the writer.

## `db.serverless` is the whole trick

`DBInstanceClass: db.serverless` is what makes an instance serverless. Any other
class silently gives you a **provisioned instance billed by the hour**, in a
cluster that otherwise looks correctly configured. This template hardcodes it.

## Deletion behaviour

`DeletionPolicy: Snapshot` on the cluster: deleting the stack takes a final
snapshot rather than either destroying the data or leaving an orphan you keep
paying for. The instances are `Delete` — they hold no data of their own, and
retaining them would leave you paying for compute attached to nothing.

## Network posture

Ingress is granted to `ClientSecurityGroupId`, not to a CIDR, so access follows
the workload rather than an address range. `PubliclyAccessible` is hardcoded
`false` on every instance.

See [`examples/basic`](examples/basic) for a parameter set you can deploy as-is.

<!-- BEGIN_CFN_DOCS -->
<!-- Generated by `./bin/cfn docs`. Do not edit between these markers. -->

### Parameters

| Parameter | Description | Type | Default |
|-----------|-------------|------|---------|
| `NamePrefix` | Prefix for the cluster and secret names. | `String` | **required** |
| `Environment` | Environment this cluster serves.<br>Allowed: `dev`, `staging`, `prod` | `String` | `dev` |
| `DatabaseName` | Initial database created inside the cluster. | `String` | `appdb` |
| `Engine` | Aurora engine.<br>Allowed: `aurora-postgresql`, `aurora-mysql` | `String` | `aurora-postgresql` |
| `EngineVersion` | Engine version. Must be one that supports Serverless v2 - 13.6+ for PostgreSQL, 3.02.0+ for MySQL. Pin it: leaving it to the default means a new cluster can come up on a different major version than the last one. | `String` | `16.4` |
| `VpcId` | VPC the cluster lives in. | `AWS::EC2::VPC::Id` | **required** |
| `SubnetIds` | Private subnets, at least two in different AZs. Aurora requires the subnet group to span multiple AZs even for a single instance. | `List<AWS::EC2::Subnet::Id>` | **required** |
| `ClientSecurityGroupId` | Security group allowed to connect, typically a Fargate service's task security group. Ingress is granted to this group rather than to a CIDR, so access follows the workload rather than the address range. | `String` | **required** |
| `MinCapacityAcu` | Minimum Aurora Capacity Units. Each ACU is roughly 2 GiB of memory with matching CPU. 0 lets the cluster pause entirely when idle - excellent for dev, and it adds a cold start of several seconds to the first query after a pause. 0.5 is the lowest always-on setting. | `Number` | `0.5` |
| `MaxCapacityAcu` | Maximum ACUs. This is the cost ceiling: at roughly $0.12 per ACU-hour, a max of 16 is about $1,400/month if it ever sustains the ceiling. | `Number` | `4` |
| `ReaderCount` | Reader instances alongside the writer. 0 is fine for dev. 1 gives you a read endpoint and, more importantly, a standby Aurora can promote in under a minute - a single-instance cluster has no failover target and recovers by creating a new instance. | `Number` | `0` |
| `BackupRetentionDays` | Automated backup retention. 1 is the minimum AWS allows; 7 is a sane floor for prod. | `Number` | `7` |
| `DeletionProtection` | Refuse to delete the cluster through the API as well as CloudFormation.<br>Allowed: `true`, `false` | `String` | `false` |
| `EnablePerformanceInsights` | Per-query wait-state analysis. Free at 7-day retention and the difference between "the database is slow" and knowing which statement is blocking.<br>Allowed: `true`, `false` | `String` | `true` |
| `MasterUsername` | Master username. The password is generated by Secrets Manager and never passes through CloudFormation - there is deliberately no password parameter here. | `String` | `dbadmin` |
| `EnableDataApi` | HTTP query endpoint, which lets a Lambda query the cluster without a VPC attachment or a connection pool. Off by default; it is a second access path to the data, authorised by IAM rather than by network position.<br>Allowed: `true`, `false` | `String` | `false` |
| `KmsKeyArn` | Customer-managed KMS key for storage encryption. Leave empty for the AWS-managed RDS key. Encryption is always on either way. | `String` | `""` |

### Outputs

| Output | Description | Exported as |
|--------|-------------|-------------|
| `ClusterEndpoint` | Writer endpoint. Always use this for writes; it follows a failover. | — |
| `ReaderEndpoint` | Reader endpoint, load-balanced across readers. With no readers it resolves to the writer, so it is safe to use unconditionally. | — |
| `ClusterPort` | Port the cluster listens on. | — |
| `ClusterIdentifier` | Cluster identifier, for the CLI and alarm dimensions. | — |
| `MasterSecretArn` | ARN of the generated master credentials. Pass this to a task definition's secrets block rather than reading the password anywhere. | — |
| `SecurityGroupId` | Security group of the cluster. | — |
| `DatabaseName` | Name of the initial database. | — |

### Resources

| Logical ID | Type | Created when |
|------------|------|--------------|
| `MasterSecret` | `AWS::SecretsManager::Secret` | always |
| `SecretAttachment` | `AWS::SecretsManager::SecretTargetAttachment` | always |
| `SubnetGroup` | `AWS::RDS::DBSubnetGroup` | always |
| `SecurityGroup` | `AWS::EC2::SecurityGroup` | always |
| `Cluster` | `AWS::RDS::DBCluster` | always |
| `Writer` | `AWS::RDS::DBInstance` | always |
| `Reader1` | `AWS::RDS::DBInstance` | `HasReaders` |
| `Reader2` | `AWS::RDS::DBInstance` | `HasSecondReader` |
<!-- END_CFN_DOCS -->

# `foundation/vpc`

A multi-AZ VPC with public and private subnets, a NAT posture you choose
explicitly, gateway and interface endpoints, and optional flow logs. Nothing else
in this library runs until this exists.

## Usage

```bash
./bin/cfn deploy foundation/vpc --env dev
```

Nested inside a stack:

```yaml
Network:
  Type: AWS::CloudFormation::Stack
  Properties:
    TemplateURL: ../../templates/foundation/vpc/template.yaml
    Parameters:
      NamePrefix: !Ref NamePrefix
      Environment: !Ref Environment
      NatGatewayMode: single
```

## The three decisions

Everything else has a defensible default. These do not.

### 1. `NatGatewayMode` — this is the cost decision

A NAT gateway is about **$32/month plus $0.045/GB processed**, and it is the
single most common surprise on an AWS bill for a small workload.

| Mode | Monthly floor | Survives an AZ failure | Use when |
|------|---------------|------------------------|----------|
| `none` | $0 | n/a — no egress at all | Workloads that only call AWS APIs. Pair with the endpoint toggles below. |
| `single` | ~$32 | No — private egress dies with the AZ | Dev and staging, and prod workloads that tolerate an egress outage. |
| `per-az` | ~$32 × AZs | Yes | Prod workloads that call the public internet on the request path. |

`single` is the default because it is the cheapest thing that works, and because
the failure it exposes (egress down in an AZ outage) is one you should choose
deliberately rather than pay for by accident.

### 2. `AvailabilityZoneCount` — 2 or 3

Two is the minimum an ALB and Multi-AZ RDS will accept. Three is what you want in
prod: losing one AZ costs a third of capacity instead of half, so the remaining
zones do not need 2× headroom.

Subnets are carved from `VpcCidr` with `Fn::Cidr` — six equal blocks, public
first, private second. There are no per-subnet CIDR parameters on purpose: six
CIDR parameters is six chances to write an overlapping range, and the failure
shows up as a routing mystery weeks later.

### 3. Endpoints — how you make `NatGatewayMode: none` work

Gateway endpoints (`EnableS3Endpoint`, `EnableDynamoDbEndpoint`) are **free** and
on by default. They also route S3 and DynamoDB traffic off the NAT gateway, so
they reduce the bill even when NAT is on.

Interface endpoints cost roughly **$7/month each per AZ**. The set that makes a
Fargate service run with no NAT at all:

| Toggle | Gives you |
|--------|-----------|
| `EnableS3Endpoint` (default on) | Image layer download — ECR stores layers in S3 |
| `EnableEcrEndpoints` | `ecr.api` + `ecr.dkr` — image manifest pull and auth |
| `EnableLogsEndpoint` | `awslogs` driver can ship container logs |
| `EnableSecretsManagerEndpoint` | Task definition secret injection |
| `EnableSsmEndpoints` | `ssm` + `ssmmessages` — ECS Exec and Session Manager |

With three AZs, ECR + Logs + Secrets is about $63/month — more than one NAT
gateway. The endpoints win when you are running several AZs' worth of traffic
through NAT, or when you need the private-only network posture regardless of
cost. Below that, `single` NAT is cheaper. Do the arithmetic for your case rather
than assuming endpoints are the frugal option.

## What it does not do

- **No exports.** Outputs are plain outputs, not `Export`ed values. A
  cross-stack `Fn::ImportValue` makes the producing stack undeletable while any
  consumer exists, which is exactly the wrong coupling for a network you may need
  to rebuild. Stacks pass these outputs down as nested-stack parameters instead;
  for manual wiring, `./bin/cfn outputs foundation/vpc --env dev`.
- **No IPv6, no Transit Gateway, no peering.** Those are per-organization
  decisions that belong in a stack, not in the primitive.
- **Flow logs are off by default.** On a busy VPC they are a real line item. Turn
  them on in prod, and anywhere you need to answer "did this actually talk to
  that".

## Interaction with other templates

Every compute template in this library takes `VpcId` and a subnet list. The
normal wiring:

| Consumer | Subnets to pass |
|----------|-----------------|
| `containers/alb` (internet-facing) | `PublicSubnetIds` |
| `containers/fargate-service` | `PrivateSubnetIds` |
| `serverless/lambda-function` (VPC-attached) | `PrivateSubnetIds` |
| `database/*` | `PrivateSubnetIds` |

See [`examples/basic`](examples/basic) for a parameter set you can deploy as-is.

<!-- BEGIN_CFN_DOCS -->
<!-- Generated by `./bin/cfn docs`. Do not edit between these markers. -->

### Parameters

| Parameter | Description | Type | Default |
|-----------|-------------|------|---------|
| `NamePrefix` | Prefix for every resource this stack names. Must be unique per account and region. | `String` | **required** |
| `Environment` | Environment this VPC belongs to. Used in names and tags.<br>Allowed: `dev`, `staging`, `prod` | `String` | `dev` |
| `VpcCidr` | IPv4 range for the VPC. A /16 gives six /24 subnets with room to grow; if this VPC will ever peer with another, pick a range that does not overlap. | `String` | `10.0.0.0/16` |
| `AvailabilityZoneCount` | How many AZs to spread across. Two is the minimum for an ALB and for Multi-AZ RDS; three is what you want in prod, because losing one AZ then costs a third of capacity rather than half.<br>Allowed: `2`, `3` | `String` | `2` |
| `NatGatewayMode` | Egress for private subnets. `none` is free and correct when workloads only talk to AWS APIs reachable through VPC endpoints. `single` is one NAT gateway shared by every AZ - cheapest working option, but an AZ failure takes egress with it. `per-az` survives an AZ failure and costs one NAT gateway per AZ (roughly $32/month each, plus data processing).<br>Allowed: `none`, `single`, `per-az` | `String` | `single` |
| `EnableS3Endpoint` | Create the S3 gateway endpoint. Gateway endpoints are free and route S3 traffic off the NAT gateway, so this usually pays for itself immediately.<br>Allowed: `true`, `false` | `String` | `true` |
| `EnableDynamoDbEndpoint` | Create the DynamoDB gateway endpoint. Also free.<br>Allowed: `true`, `false` | `String` | `true` |
| `EnableEcrEndpoints` | Create the ecr.api and ecr.dkr interface endpoints. Together with the S3 gateway endpoint (which serves the image layers), these are what let a Fargate task pull an image with NatGatewayMode=none.<br>Allowed: `true`, `false` | `String` | `false` |
| `EnableLogsEndpoint` | Create the CloudWatch Logs interface endpoint. Required for tasks in private subnets with no NAT to ship logs.<br>Allowed: `true`, `false` | `String` | `false` |
| `EnableSecretsManagerEndpoint` | Create the Secrets Manager interface endpoint. Required for a task definition that injects secrets when there is no NAT.<br>Allowed: `true`, `false` | `String` | `false` |
| `EnableSsmEndpoints` | Create the ssm and ssmmessages interface endpoints, which is what ECS Exec ("docker exec into a Fargate task") and Session Manager need without NAT.<br>Allowed: `true`, `false` | `String` | `false` |
| `EnableFlowLogs` | Send VPC flow logs to CloudWatch Logs. Off by default because flow logs on a busy VPC are a real line item; turn them on in prod and anywhere you need to answer "did this actually talk to that".<br>Allowed: `true`, `false` | `String` | `false` |
| `FlowLogTrafficType` | Which flows to record.<br>Allowed: `ACCEPT`, `REJECT`, `ALL` | `String` | `REJECT` |
| `FlowLogRetentionDays` | Retention for the flow log group.<br>Allowed: `1`, `3`, `5`, `7`, `14`, `30`, `60`, `90`, `120`, `150`, `180`, `365`, `400`, `545`, `731`, `1827`, `3653` | `Number` | `30` |

### Outputs

| Output | Description | Exported as |
|--------|-------------|-------------|
| `VpcId` | ID of the VPC. | — |
| `VpcCidrBlock` | IPv4 range of the VPC, for security group rules in other stacks. | — |
| `PublicSubnetIds` | Comma-separated public subnet IDs, for internet-facing load balancers. | — |
| `PrivateSubnetIds` | Comma-separated private subnet IDs, for tasks, functions and databases. | — |
| `AvailabilityZones` | Comma-separated AZs this VPC spans. | — |
| `PrivateRouteTableIds` | Comma-separated private route table IDs, for adding peering or endpoint routes. | — |
| `InterfaceEndpointSecurityGroupId` | Security group attached to the interface endpoints, when any were created. | — |
| `HasNatGateway` | Whether private subnets have a default route to the internet. | — |

### Resources

| Logical ID | Type | Created when |
|------------|------|--------------|
| `Vpc` | `AWS::EC2::VPC` | always |
| `InternetGateway` | `AWS::EC2::InternetGateway` | always |
| `InternetGatewayAttachment` | `AWS::EC2::VPCGatewayAttachment` | always |
| `PublicSubnet1` | `AWS::EC2::Subnet` | always |
| `PublicSubnet2` | `AWS::EC2::Subnet` | always |
| `PublicSubnet3` | `AWS::EC2::Subnet` | `HasThreeAzs` |
| `PrivateSubnet1` | `AWS::EC2::Subnet` | always |
| `PrivateSubnet2` | `AWS::EC2::Subnet` | always |
| `PrivateSubnet3` | `AWS::EC2::Subnet` | `HasThreeAzs` |
| `PublicRouteTable` | `AWS::EC2::RouteTable` | always |
| `PublicDefaultRoute` | `AWS::EC2::Route` | always |
| `PublicSubnet1RouteTableAssociation` | `AWS::EC2::SubnetRouteTableAssociation` | always |
| `PublicSubnet2RouteTableAssociation` | `AWS::EC2::SubnetRouteTableAssociation` | always |
| `PublicSubnet3RouteTableAssociation` | `AWS::EC2::SubnetRouteTableAssociation` | `HasThreeAzs` |
| `NatEip1` | `AWS::EC2::EIP` | `HasNat` |
| `NatGateway1` | `AWS::EC2::NatGateway` | `HasNat` |
| `NatEip2` | `AWS::EC2::EIP` | `HasNatPerAz` |
| `NatGateway2` | `AWS::EC2::NatGateway` | `HasNatPerAz` |
| `NatEip3` | `AWS::EC2::EIP` | `HasNatInAz3` |
| `NatGateway3` | `AWS::EC2::NatGateway` | `HasNatInAz3` |
| `PrivateRouteTable1` | `AWS::EC2::RouteTable` | always |
| `PrivateRouteTable2` | `AWS::EC2::RouteTable` | always |
| `PrivateRouteTable3` | `AWS::EC2::RouteTable` | `HasThreeAzs` |
| `PrivateDefaultRoute1` | `AWS::EC2::Route` | `HasNat` |
| `PrivateDefaultRoute2` | `AWS::EC2::Route` | `HasNat` |
| `PrivateDefaultRoute3` | `AWS::EC2::Route` | `HasPrivateRouteInAz3` |
| `PrivateSubnet1RouteTableAssociation` | `AWS::EC2::SubnetRouteTableAssociation` | always |
| `PrivateSubnet2RouteTableAssociation` | `AWS::EC2::SubnetRouteTableAssociation` | always |
| `PrivateSubnet3RouteTableAssociation` | `AWS::EC2::SubnetRouteTableAssociation` | `HasThreeAzs` |
| `S3GatewayEndpoint` | `AWS::EC2::VPCEndpoint` | `ShouldCreateS3Endpoint` |
| `DynamoDbGatewayEndpoint` | `AWS::EC2::VPCEndpoint` | `ShouldCreateDynamoDbEndpoint` |
| `InterfaceEndpointSecurityGroup` | `AWS::EC2::SecurityGroup` | `HasInterfaceEndpoints` |
| `EcrApiEndpoint` | `AWS::EC2::VPCEndpoint` | `ShouldCreateEcrEndpoints` |
| `EcrDockerEndpoint` | `AWS::EC2::VPCEndpoint` | `ShouldCreateEcrEndpoints` |
| `LogsEndpoint` | `AWS::EC2::VPCEndpoint` | `ShouldCreateLogsEndpoint` |
| `SecretsManagerEndpoint` | `AWS::EC2::VPCEndpoint` | `ShouldCreateSecretsEndpoint` |
| `SsmEndpoint` | `AWS::EC2::VPCEndpoint` | `ShouldCreateSsmEndpoints` |
| `SsmMessagesEndpoint` | `AWS::EC2::VPCEndpoint` | `ShouldCreateSsmEndpoints` |
| `FlowLogGroup` | `AWS::Logs::LogGroup` | `ShouldCreateFlowLogs` |
| `FlowLogRole` | `AWS::IAM::Role` | `ShouldCreateFlowLogs` |
| `FlowLog` | `AWS::EC2::FlowLog` | `ShouldCreateFlowLogs` |
<!-- END_CFN_DOCS -->

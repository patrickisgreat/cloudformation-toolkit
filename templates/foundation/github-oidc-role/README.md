# `foundation/github-oidc-role`

An IAM role GitHub Actions assumes over OIDC, scoped to one repository and a
chosen set of refs. This is how you delete the `AWS_SECRET_ACCESS_KEY` from your
repository secrets.

## Usage

```bash
./bin/cfn deploy foundation/github-oidc-role --env dev \
  --param GitHubOrg=your-org --param GitHubRepo=your-repo
```

Then in the workflow:

```yaml
permissions:
  id-token: write      # without this, no OIDC token is minted and the step fails
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::<account>:role/<NamePrefix>-github-deploy
          aws-region: us-east-1
      - run: ./bin/cfn deploy container-service --env dev --yes
```

`permissions: id-token: write` is the step everyone forgets. Without it the
runner never receives a token, and `configure-aws-credentials` fails with a
message about a missing `ACTIONS_ID_TOKEN_REQUEST_URL`.

## The trust policy is the whole security model

Two conditions do the work, and both are mandatory:

- **`aud` = `sts.amazonaws.com`** — stops a token minted for some other relying
  party from being replayed against STS.
- **`sub` matches your repo and ref** — stops *every other repository on GitHub*
  from assuming the role. A trust policy with only the audience check is a
  well-known catastrophic misconfiguration: GitHub is the issuer for every public
  repository in the world.

The subject is scoped from strongest to weakest:

| Setting | Subject matched | Notes |
|---------|-----------------|-------|
| `AllowedEnvironment` | `repo:org/repo:environment:production` | Strongest. A GitHub Environment can require manual approval before the job runs. |
| `AllowedBranch` (default `main`) | `repo:org/repo:ref:refs/heads/main` | Supports a trailing wildcard, e.g. `release/*`. |
| `AllowPullRequests` | `repo:org/repo:pull_request` | **Off by default.** A PR from a fork runs code its author controls, and this role can deploy. |

For production, prefer `AllowedEnvironment` with a required reviewer over a
branch match. A branch match is only as strong as your branch protection.

## One provider per account

`AWS::IAM::OIDCProvider` for `token.actions.githubusercontent.com` is a
singleton per AWS account. The first stack creates it; every stack after that
sets `CreateOidcProvider=false` and passes `ExistingOidcProviderArn`. Creating a
second one fails with `EntityAlreadyExists` partway through the deploy.

## What the role can do

`GrantCloudFormationDeploy` attaches an inline policy for driving stacks —
change sets, updates, describes — scoped to stacks named `<Environment>-*`, which
matches the naming `bin/cfn` uses. It deliberately does **not** grant permission
to create the underlying resources. Give it those separately via
`ManagedPolicyArn`, so the deploy permission and the resource permission are two
reviewable decisions rather than one.

See [`examples/basic`](examples/basic) for a parameter set you can deploy as-is.

<!-- BEGIN_CFN_DOCS -->
<!-- Generated by `./bin/cfn docs`. Do not edit between these markers. -->

### Parameters

| Parameter | Description | Type | Default |
|-----------|-------------|------|---------|
| `NamePrefix` | Prefix for the role name. | `String` | **required** |
| `Environment` | Environment this role deploys into.<br>Allowed: `dev`, `staging`, `prod` | `String` | `dev` |
| `GitHubOrg` | GitHub organisation or user that owns the repository. | `String` | **required** |
| `GitHubRepo` | Repository name, without the owner. The trust policy is scoped to exactly this repository - a wildcard here would let any repo in the org assume the role. | `String` | **required** |
| `AllowedBranch` | Branch whose workflows may assume the role. Supports a trailing wildcard ("release/*"). Set to "*" only if you understand that it lets any branch, including one pushed by a fork-triggered workflow, deploy. | `String` | `main` |
| `AllowedEnvironment` | GitHub Environment name whose jobs may assume the role, e.g. "production". This is the strongest scoping GitHub offers, because an environment can require a manual approval before the job runs. Leave empty to skip. | `String` | `""` |
| `AllowPullRequests` | Let pull-request workflows assume the role. Off by default: a PR from a fork runs code the author controls, and this role can deploy.<br>Allowed: `true`, `false` | `String` | `false` |
| `ManagedPolicyArn` | Managed policy to attach, e.g. arn:aws:iam::aws:policy/PowerUserAccess or your own least-privilege deploy policy. Leave empty to rely solely on the built-in CloudFormation deploy permissions below. | `String` | `""` |
| `GrantCloudFormationDeploy` | Attach an inline policy allowing this role to run `cfn deploy` - change sets, stack updates, and reading template artifacts from the bucket below. It intentionally does not grant permission to create the underlying resources; CloudFormation does that with its own service role or with these credentials plus a managed policy.<br>Allowed: `true`, `false` | `String` | `true` |
| `ArtifactsBucketArn` | ARN of the S3 bucket that `cfn deploy` packages nested templates into. Required when GrantCloudFormationDeploy is true and you deploy stacks with nested templates. | `String` | `""` |
| `PermissionsBoundaryArn` | Optional permissions boundary. In an organisation that uses boundaries, omitting one here is usually what makes the role creation fail. | `String` | `""` |
| `CreateOidcProvider` | Create the GitHub OIDC provider in this account. There can only be one per account, so set this to false in every stack after the first.<br>Allowed: `true`, `false` | `String` | `true` |
| `ExistingOidcProviderArn` | ARN of the existing GitHub OIDC provider, when not creating one. | `String` | `""` |
| `MaxSessionDurationSeconds` | Maximum session length. One hour covers almost every deploy; raise it only if a single job genuinely runs longer, since the credential lives that long. | `Number` | `3600` |

### Outputs

| Output | Description | Exported as |
|--------|-------------|-------------|
| `RoleArn` | Role ARN for the workflow's aws-actions/configure-aws-credentials step (role-to-assume). | — |
| `RoleName` | Name of the deploy role. | — |
| `OidcProviderArn` | ARN of the OIDC provider, when this stack created one. | — |

### Resources

| Logical ID | Type | Created when |
|------------|------|--------------|
| `OidcProvider` | `AWS::IAM::OIDCProvider` | `ShouldCreateOidcProvider` |
| `Role` | `AWS::IAM::Role` | always |
<!-- END_CFN_DOCS -->
